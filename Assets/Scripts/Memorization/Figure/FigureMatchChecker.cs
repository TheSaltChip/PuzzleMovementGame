using UnityEngine;

namespace Memorization.Figure
{
    public class FigureMatchChecker : MonoBehaviour
    {
        /* TODO: Use selected figures SO to check if the selected figures are matching
         * Check if the number of selected items are equal to a limit
         * Subscribe to an event which fires whenever a figure has been selected
         * If the items are matching delete and remove from list
         * if not reenable collider and remove from list
         */
    }
}